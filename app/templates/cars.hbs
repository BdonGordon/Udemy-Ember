<h2>Cars Post</h2>

<ul>
  {{#each model as |car|}}
  {{!--Looking at the #link-to, we are passing the url id like so:--}}
  <li><strong>{{car.make}} {{car.model}}</strong> {{car.year}} - {{#link-to "cars.edit" car.id}}Edit{{/link-to}}</li>
  {{/each}}
</ul>

{{#link-to "cars.new"}}Add a new car{{/link-to}}

{{outlet}}