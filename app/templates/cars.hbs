<h2>Cars Post</h2>

<ul>
  {{#each model as |car|}}
  <li>
    {{!--Looking at the #link-to, we are passing the url id like so: "cars.edit" car.id --}}
    <strong>{{car.make}} {{car.model}}</strong> {{car.year}} - {{#link-to "cars.edit" car.id}}Edit{{/link-to}}
    {{!--Delete car is an action in controllers/car.js that requires the car ID parameter; passed like so:--}}
    <button {{action 'deleteCar' car.id}}>Delete</button>
  </li>
  {{/each}}
</ul>

{{#link-to "cars.new"}}Add a new car{{/link-to}}

{{outlet}}